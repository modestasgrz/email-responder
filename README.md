Yet to be developed

## GCP Setup

Before deploying, run `make gcp-setup` to create the Artifact Registry Docker repository and authenticate Docker with GCP. This is a one-time setup step — see DESIGN.md for full deployment details.
